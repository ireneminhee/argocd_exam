apiVersion: apps/v1
kind: Deployment
metadata:
  name: echo-hostname
  namespace: default
spec:
  replicas: 2
  selector:
    matchLabels:
      app: echo-hostname
  template:
    metadata:
      labels:
        app: echo-hostname
    spec:
      containers:
        - name: echo-hostname
          image: ireneminhee/echo-hostname:v1.0
          ports:
            - containerPort: 5000
